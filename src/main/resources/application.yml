javafx:
  title:  ${JavaFX.user}
  appicons: /icon/icon.png

  stage:
    width: 1378
    height: 835
    resizable: false

  # "jdbc:h2:file:D:/test"  连接自定目录下的指定数据库
  # "jdbc:h2:~/test" 连接默认目录下的指定数据库
  # jdbc:h2:[file:][<path>]<databaseName>
  # jdbc:h2:~/test
  # jdbc:h2:file:${java.io.tmpdir}sample
  # jdbc:h2:file:C:/data/sample (Windows only)
spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
      #  schema:
    #   - classpath:/db/schema.sql
    url: jdbc:h2:file:${user.dir}/temp/db/H2
    driverClassName: org.h2.Driver
    username: sa
    password: sarbr
    continue-on-error: true
    initialization-mode: NEVER
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true
  jpa:
    database: h2
    hibernate:
      ddl-auto: none
    show-sql: true
